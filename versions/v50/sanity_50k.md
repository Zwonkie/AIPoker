# V50 training sanity checks (every ~50k hands)

Raw `active_training.log` telemetry block captured at each 50k crossing, plus a computed one-line sanity read. Newest sections appended at the bottom.
Battery candidates: the `main_hands*.pth` under `weights/checkpoints/`.

---
name: ofk-memory-manager
description: |
  Manages the project's Open Knowledge Format (OFK) memory. Use this skill 
  whenever creating, updating, reading, or indexing persistent memory files 
  and knowledge items in the workspace.
---
# Open Knowledge Format (OFK) Memory Management Guidelines
This skill guides the agent on how to interact with and maintain the workspace's persistent memory layer. The goal is to prevent repetitive context-gathering and ensure resolved bugs or design decisions are permanently recorded.
## 1. Directory Structure
The memory base is organized as follows:
- `SKILL.md`: The entry point, index, and rules for memory management.
- `references/`: A directory containing individual markdown knowledge files categorized by topic (e.g., `references/api-integration.md`, `references/resolved-bugs.md`).
## 2. When to Create or Update Memory
At the end of a task or chat session, the agent should evaluate if a new memory should be written or updated. You **MUST** record information if:
- **Critical Bugs Resolved**: You spent significant time debugging a complex issue or an obscure error. Document the root cause and the fix.
- **Architectural Decisions**: A design choice was made (e.g., *"Why we used library X instead of Y"*).
- **Environment Setup**: Complex setup steps or local dependencies are configured.
- **APIs and Schemas**: Crucial request/response shapes or DB schemas are identified.

## 2.5 Bot models (NN) and version management
Anything related to bot models (NN) and version management (retrain, issues, fixes, and so on) should have its own OFK ressource folder under that specific bot version it relates to. E.g. if you are working on the V8 bot model, you should create a folder named `V8` under the `references/` directory and store all related memory files in it. This should also apply to subversions of the bot model, such as `V8.1`, `V8.2`, etc. information can be catogorized into the following: "model_specifications" (NN model architectures, training parameters, etc.), "issues and fixes" (known issues, bugs, suggestion on how to improve it, for the next version) 

## 3. Formatting Memory Files (OKF Standard)
All memory files in the `references/` directory must follow these rules:
- **File Format**: Standard markdown with a `.md` extension.
- **File Name**: Descriptive, lowercase, and hyphenated (e.g., `database-setup.md`).
- **Structure**:
  ```markdown
  # [Descriptive Title]
  
  **Date Recorded**: YYYY-MM-DD
  **Related Files**: [main.go](file:///path/to/main.go)
  
  ## Context
  Provide 2-3 sentences explaining the background or the problem.
  
  ## Resolution / Guidelines
  Provide the specific steps, code patterns, or commands required to handle this topic.
  ```

## 4. Memory Directory Index
*(Cleaned 2026-07-15: merged the V4–V10 per-version specs/reports into `legacy-versions-history.md`; deleted point-in-time health reports for dead models, the self-superseded versioning-standards, the stale 31-dim data-contract doc, and the realized V12 design-proposal. Updated 2026-07-17: added the cross-version Known Shortcomings backlog. **Updated 2026-07-21: current live model = V41**; added the Fable-review section and V41's report; corrected stale "CURRENTLY LIVE" labels on V15 and V20_preflopEq_AI. **Known gap: V21–V29 are not indexed in the version list below** — read `versions/<vN>/SPECS.md` directly for those.)*

### ⭐ Known Shortcomings (cross-version, check this FIRST)
*   ⭐ [Known Shortcomings — Tracked Backlog](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/known-shortcomings-backlog.md): **Standing catalog of identified model weaknesses, independent of which version is active.** Before diagnosing a new live-play or training issue, check here first — it's very often a known item resurfacing, not a fresh discovery. Categories: betting behavior (shove-preference/no-middle-gear, short-stack polarization), stack depth (deep-stack OOD trash-jam, beyond-50bb extrapolation), opponent modeling (no per-opponent action attribution, size-blind history, no full hand history — hero's decision points only, live front/after reopened-action blindness), format fit (no ICM), and validation/tooling gaps (no solver ground truth, cross-scale predecessor comparison gap). Each entry: plain-language description, technical root cause, suggestion, status (🔴 OPEN / 🟡 PARTIAL / 🟢 RESOLVED), first-identified + last-confirmed version. Update statuses when a new version's results bear on an existing entry — don't just append a duplicate.

### Fable full-stack review (2026-07-20, V29 audit — cross-references the backlog, read alongside it)
*   ⭐ [Fable Review — RESOLUTION LOG](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/fable-review-resolution-log.md): **Read this before acting on any `fable-review-*` finding — it says what is already fixed.** Side-by-side status tracker (FIXED / PARTIAL / OPEN / NOT-A-DEFECT / DEFERRED) for every numbered finding. The `fable-review-*` docs are the reviewer's own words and are NEVER edited; all outcomes are recorded here instead. Outcome: findings #1/#2/#3 → V40, #7-#11 → V41, #14/#15/#16 (live serving) + #4 (tooling) fixed in place; **V41 deployed live 2026-07-21, [BET-3] RESOLVED**. Tier 5 = **V42_liveFixes** (`versions/v42_liveFixes/SPECS.md`, live layer only, no retrain): #13 + M4/M6 (money/OCR integrity) and #6/#8/#10/#12-CE (train↔serve encoding) — and it caught, in production, the #16/H4 remainder those ladders warned about: **V41 was being served vs-random equity and a constant `hand_strength`**. Engines now declare `live_features()` alongside `make_bridge()`. Also records why the deferred items were deferred, and the H4 "deploy is N hand-synchronized edits that fail silently" lesson (demonstrated twice in one day).
*   ⭐ [Fable Review — Consolidated](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/fable-review-consolidated.md): Ranked synthesis of a 4-area critical audit of the V29 pipeline, with per-claim verification status (which findings were independently confirmed vs reviewer-reported) and a triage order. Headlines: betting round ends on any check (zero post-check training data — [BET-3] candidate); CALL exempt from the V28 variance penalty; ALLIN veto self-confirms ([STACK-3] mechanism); `beats_frozen_predecessor` never actually seats the frozen model; live all-PAD action history; call-button OCR miss force-masks FOLD.
*   [Fable Review — Contract/Encoding](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/fable-review-contract-encoding.md): Rollout encoder drift (slot-index `is_active`, placeholder opponent stacks), [OPP-7] remap defeated at the tensor boundary (`seat_0` keys unreadable), `contract_version` never validated (width-only), live-vs-training default mismatches, sentinel collisions, short-handed position arithmetic.
*   [Fable Review — Simulation](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/fable-review-simulation.md): Betting-round state machine bug (empirically confirmed, 1000-hand instrumented run), all opponent raises hardcoded 0.75-pot, dead blinds (pre-fold before posting), NN opponents fed corrupted call_amount + vs-random equity, min-raise rule deviations, latent negative-`to_call` bug masked by symmetric stacks.
*   [Fable Review — Training/Eval](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/fable-review-training.md): Vacuous predecessor/field checks (dead `past_model` attrs — v17_gauntlet class bug recurring), CALL-exempt risk penalty, four stacked un-jointly-calibrated risk dampeners, 0.15bb veto margin below critic noise, no confidence intervals on BB/100 gates, gameplay_eval temp mismatch, Goodharting `deep_stack_ood_guard`.
*   [Fable Review — Live Serving](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/fable-review-live-serving.md): call_amount=0.0 OCR-miss → forced-no-fold, random-weights-on-load-failure, all-PAD live action history, three `is_vN` dispatch ladders w/ V20 fallback + silent fold-every-hand crash path, frame-merge raise misattribution, pot-reset/latch glitches, blinds excluded from `committed`, decimal-stake unit mixing.

### Architecture & process
*   ⭐ [Versioned Architecture Guardrails (CANONICAL)](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/versioned-architecture-guardrails.md): **Read first before touching any model, contract, simulator, weight, or live-play code.** Binding Golden Rules for isolating each version — manifest/registry dispatch, self-describing fail-loud weights, live/training contract unification, the "start a new version" checklist. §0 tracks the current foundation (V15).
*   [Workspace Architecture Overview](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/workspace-architecture-overview.md): The 4-layer partition (Dashboards → BoardState → Contract → ModelEngine) + the three-sandbox rule. (Examples reference the older ContractV8V9; the layering itself is current.)
*   [Pipeline Flow (Sim/Train vs Live)](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/pipeline-flow.md): Two maintained Mermaid diagrams of every condition/logic/data tweak in the sim/training pipeline and the live path, colour-coded by scope (global/board/player-state), with ID'd boxes + a train≡serve invariants checklist. Update on any pipeline tweak.
*   [Simulation Architecture (deep dive)](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/simulation_architecture.md): Prose companion to the pipeline diagram — opponent pool/weights, 5-phase curriculum, bootstrap decay, HandRecord, and the per-head counterfactual+realized Q-target math. (V10/V11-era internals; the 6-action/ContractV12 specifics live in the V13–V15 specs.)

### Live play & infra
*   [V46_legacySweep — pre-v30 legacy retirement](file:///c:/REPO/Antigravity/AIPoker/versions/v46_legacySweep/SPECS.md): **2026-07-22, user policy: versions before v30 are LEGACY.** Registry = v44/v43/v41/v40 only (V29 deregistered); decision.py's `is_vN` ladders, per-version bridges, `_LEGACY_LIVE_FEATURES`, display-tag ternary, v9 guardrail and math/bluff/chart override layers DELETED — dispatch is 100% engine-declared, undeclared models refuse loudly. Also: `PokerModelInterface` ABC retired, PHPHelp dead code removed, tracked `__pycache__`/logs/telemetry untracked + gitignored, `tools/self_play` → `attic/`. Verified 31/31 + full suite battery green. Closes Fable finding #16/H4.
*   ⭐ [V45_liveHandover — the live↔model boundary](file:///c:/REPO/Antigravity/AIPoker/versions/v45_liveHandover/SPECS.md): **How models hook to the live dashboard now (2026-07-22).** `TableState.to_observation()` → frozen RAW `LiveObservation` (`core/live_observation.py`: None-sentinels, append-only schema, JSON round-trip) → version-owned `BaseLiveAdapter.decide()` (`core/live_adapter.py`: equity/front-after/hand_strength/effective_field per the engine's own `live_features()`) → untouched `make_decision`. PHPHelp has zero per-version logic on the decision path; a new version = engine declarations + registry entry, no dashboard edit. Parity-verified byte-identical (`verify_handover.py` 14/14 + V42 suites green). turns.jsonl now records the raw observation for offline replay. Live diagram updated in [pipeline-flow.md](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/pipeline-flow.md) (new LB5/LD0 boxes).
*   [Decision Pipeline Tracing & GUI Overrides](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/decision-pipeline-tracing-and-gui-overrides.md): Live-play mechanics — decision_path trace, the ⚡ Auto-Live quick-start + window matching, cents-basis blind parsing from the window title, hero-position tracking, and the live "Thinking" narrative (§7, 2026-07-15 — equity+action-banded human-readable read, deliberately NOT sourced from the untrained/opponent-read aux heads). (Default-model/action-space framing predates the 6-action space.)
*   [HUD Thresholds & Aggression](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/hud-thresholds-and-aggression.md): Canonical VPIP/AFq HUD colour bands (Blue/Green/Yellow/Red) + the colour→midpoint vectorization map. Consumed by both live HUD reads and sim opponent modelling.
*   ⭐ [Model Verification Suite](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/model-verification-suite.md): **The current standing tool — `tools/model_verify`.** Version-agnostic fast scenario checks (deep-stack OOD guard, equity-ablation monotonicity, action-collapse guard, etc.) + slow simulated-hand checks (`--full`: VPIP-vs-style, BB/100-vs-baseline, beats-frozen-predecessor), a growing append-only curriculum, and an HTML raw-data report generator. Run after every training run. Documents 2 calibration bugs (substring-match trap, over-strict diversity threshold) and a live finding (V15's deep-stack OOD trash-jam is still present under decision-level probing despite passing aggregate evals).
*   [Model Testing Suite (superseded)](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/model-testing-suite.md): V4-V6 era manual/ad hoc sensitivity scenarios (72o-shove extrapolation, air bluffs, nutted traps, equity monotonicity), predates the 6-action contract and isn't committed as runnable code. Superseded by Model Verification Suite above; kept only for historical scenario-design ideas.
*   [Windows PyTorch Multiprocessing Deadlock](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/windows-pytorch-multiprocessing-deadlock.md): Version-agnostic infra fix — keep the worker Pool alive (reuse via starmap) + ship weights via a temp .pth, to avoid the spawn+CUDA re-init hang.
*   [Monitor Training Session Skill](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/monitor-training-session/SKILL.md): SOP for evaluating live training telemetry, detecting rigidity, and stopping early collapses. Dashboard tool itself lives standalone at `tools/training_monitor/` (not skill-owned).

### History
*   [Legacy Versions History (V4–V10)](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/legacy-versions-history.md): One paragraph per superseded version — durable lessons only (off-policy EV hallucination, attention/padding collapse, all-action loss, terminal-state value inflation, stack-size curriculum, collapse-detection telemetry). Tables/point-in-time reports dropped.

### Current model line (V11 bridge → **V41 live**)

> ⚠️ **This list is INCOMPLETE below.** It runs V11 → V20_preflopEq_AI and then stops; **V21–V29 are
> not indexed here at all**, and older entries still carry stale "CURRENTLY LIVE" labels from when
> they were written. The live model is **V41** (see the entry at the end of this list). For any
> version from V21 on, read `versions/<vN>/SPECS.md` in the repo directly, and check
> `core/decision.py`'s `active_model_name` if you need to know what is actually serving. Backfilling
> V21–V29 into this index is an open task.
*   [V11 Model Specifications](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V11/model-specifications.md): The bridge into the current architecture — 35-feature contract, counterfactual policy targets, causal masking, left-padding, aux heads, fuzzy heuristics. (Retained standalone — not folded into the legacy history.)
*   [V11 Issues & Fixes](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V11/issues-and-fixes.md): The richest catalogue of still-live bug patterns — unit-scale (chips vs BB) mismatch, train/infer padding parity, `idx` shadowing, arch↔weight shape mismatch, silent-heuristic fallback, loose-collapse ratchet, off-policy raise-everything, atomic weight saves.
*   [V12 Validated Findings](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V12/validated-findings.md): The frozen v12_validated baseline — locked fixes (target clip 40, counterfactual policy target, equity-primary arch, realization discount, postflop-data field) + the safe-change workflow.
*   [V12 Issues & Fixes](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V12/issues-and-fixes.md): Two durable V12 gotchas — the `key_padding_mask` NaN/OOD trap and the train↔ContractV12 context-alignment mismatch.
*   [V13 (specs / milestone / validated-findings)](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V13/specs.md): Equity-primary + range-aware equity (opponent adaptation); first live-viable MILESTONE foundation. Folder also has `milestone.md`, `validated-findings.md`.
*   [V14 Specs](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V14/specs.md): Discretized 6-action bet-size space {fold,call,raise_33/66/pot,allin}; short-stack winner; later found to have deep-stack OOD.
*   [V15 Specs](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V15/specs.md): **WAS LIVE 2026-07-15; long superseded** (label corrected 2026-07-21 — V41 is live). Same 6-action contract retrained on a DoN-shaped stack mixture (5–50bb) + a frozen-V14 opponent; fixes the deep-stack OOD; loose-aggressive winner vs loose fields.
*   [V16 Roadmap / Backlog](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V16/specs.md): **P2 done** (stack-scaled live temp). **P4 trained** (preflop CALL/FOLD target swapped from oracle equity to range-aware equity — single substitution, no new tuned constants, no contract bump); V16 itself is trained but NOT yet deployed live (V15 still active/default). **P3 deferred** (later CONFIRMED resolved via model_verify — see V17). Also: P5/P6 (input-contract gaps), size-scaled bluffing, a RECONFIRMED V15 deep-stack-OOD finding (P0-recheck, V16 inherits it), P7 (opponent-pool "Yellow"/LAG training gap), and the live "Thinking" narrative feature (equity+action-banded HUD line, `core/decision.py`/`PHPHelp.py`). **`v16_foldregret` sub-experiment trained + evaluated, NOT deployed** (2026-07-15): fold-relative regret baseline fixed the air/draws free-riding continuation problem cleanly, but flipped the model's style (tight_deep fixed, loose_deep collapsed vs the actual loose-heavy live population) — kept as a reference result, root-cause analysis superseded it (see V17).
*   [V17 Specs — superseded](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V17/specs.md): Round 1 (mean-baseline critic routing) rejected — diluted FOLD even against a well-calibrated critic, wrong-direction VPIP drift. Round 2 (fold-relative baseline on the DENOISED critic Q, not raw noisy mc_evs) fixed it: `model_verify --full` 10 PASS/1 WARN/1 FAIL (FAIL = the same pre-existing deep-stack OOD every version carries). Fixes air/draws overcontinuation (air_folds_mostly 0.62->1.00) with NO style-flip regression (loose_deep BB/100 +62.1->+90.3, beats V16 outright; foldregret had collapsed this to -11.6). Also: telemetry bug found+fixed (free checks were silently conflated with paid air-calls in the equity-matrix dashboard) and an equity_sims MC cost/noise analysis (recommend reverting 5000->2000 for V18 — real ~2.5x CPU cost, marginal noise benefit). Superseded by V17_gauntlet, then V19.
*   [V17_gauntlet Specs — superseded](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V17_gauntlet/specs.md): Widened opponent pool (frozen V15 in `nit`, intended frozen V16 in `tag`, true lagged self-play mirror in `past`). **CORRECTION found 2026-07-16**: a stray leftover line (`opp_model = self.tag_model` immediately followed by an accidental `opp_model = None`) silently nullified the `tag` seat's model load for the entire 200k-hand run — frozen V16 was never actually queried, `tag` fell back to the TAG heuristic the whole time. Doesn't invalidate the deployed checkpoint's own measurements, just means "3 skilled opponents" was overstated (it was 2). This bug is what triggered the V18 opponent-architecture refactor. Superseded by V19.
*   [V18 Specs — opponent-architecture refactor](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V18/specs.md): Pure plumbing refactor (not a training experiment) triggered by the V17_gauntlet `tag`-seat bug — replaced 5 separate `self.<style>_model` attrs + a hardcoded `elif` chain with a uniform `Opponent`/`HeuristicOpponent`/`NNOpponent` interface (`self_play/opponents.py`) + a declarative `opponents.pool` config, structurally preventing that class of bug from recurring. Dashboard seat labels changed to show actual bot identity + type (`"{BotName} ({Heuristic|NN})"`), not the archetype-slot name. Verified via a 20k-hand sanity run (PASS); never itself trained to full production scale — V19 is the first full training pass on this plumbing. Carries forward the full backlog: [P0] deep-stack OOD (top priority, unaddressed 5 versions running at the time), [P5]/[P6] input-contract gaps, a `hero_position`-never-set bug found while investigating, and the open Past-Self VPIP mystery.
*   [V19 Specs — superseded](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V19/specs.md): Three targeted fixes on top of V18's plumbing: **[P0]** the preflop opponent fold-bar was size-blind (`decide_preflop` never used `pot_odds`, unlike `decide_postflop`'s V14 [P1b] fix) — a min-raise and an all-in shove got the identical simulated fold rate, inflating the all-in EV target; fixed to mirror the postflop pattern. **[hero_position]** every training-time model query (Hero's own AND every opponent's) silently defaulted to Button position (`BoardState.hero_position` never set in `_query_model_decide`) — confirmed universal across V12-V18, training-only (live serve was always correct); fixed by threading each actor's real button-relative position through. **[Past-Self mystery]** investigated not fixed — two real asymmetries found (Hero's permanent 15% heuristic-anchor floor `past` never has; opponent queries get a wrong/incomplete table view that omits Hero and includes themselves) but don't fully explain the gap; flagged for a future seat-loop rework. `model_verify --full`: 10 PASS/1 WARN/1 **FAIL** — `deep_stack_ood_guard` NOT fixed by [P0]; the full failure grid (13/25 cells, eq>=0.43 across the whole 15-40bb range) is roughly FLAT across stack depth rather than scaling with it — the flatness itself became the lead for V20 (below). Superseded by V20.
*   [V20 Specs — superseded](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V20/specs.md): SUPERSEDED by V20_preflopEq (2026-07-17), kept live as a rollback in `core/decision.py`. (`core/models/v20_engine.py`, `Herocules (v20)`). V19's `policy_tightness_bb` threshold hypothesis was RE-EXAMINED and DISPROVEN by computing its own numbers (discount is literally zero at eq=0.48/0.55, exactly where the failure is worst). The real lead: `contract.py`'s stack/pot/call_amount context features were normalized against a hypothetical ~400bb range while `stack_depth_mix` has capped real training stacks at 5-50bb since V15 — a 15bb vs 40bb stack landed only 0.0625 apart on a [0,1] feature. Rescaled ctx[1]/ctx[2]/ctx[9]+opp_stack from /400(/1000) to /100(/250) — 4x resolution gain, contract_version bumped 3→4 (own live bridge required, can't share `bridge_v13`). Trained 120k hands (nit/tag reverted to plain heuristics — a rescaled contract can't safely query externally-frozen old-scale checkpoints as opponents without a per-model contract-selection mechanism, out of scope this pass). `model_verify --full`: first run 8 PASS/1 WARN/2 FAIL/1 SKIP; re-run with a fresh MC seed flipped `vpip_adapts_to_style` to PASS (was 4.9pt vs the 5pt gate, MC noise not a real gap) → **9 PASS/1 WARN/1 FAIL/1 SKIP**. `deep_stack_ood_guard` still fails but shows the first genuine stack-depth differentiation this whole model line has produced (eq=0.43: argmax flips ALL-IN→raise_33 at 30-40bb; V19's grid was completely flat) — partial progress, not resolved. **Live-safety issue found+fixed pre-deployment**: the resolution gain traded away OOD headroom — a flopped set of aces at real table depths beyond 50bb showed fold% climbing with depth (10%→32% from 45bb→150bb), a genuine extrapolation artifact. Fixed with a live-serve clamp on the stack-derived context features to the training ceiling (verified: fold rate now flat ~13% from 20bb-150bb+). Deployed at 120k from a preserved snapshot (`expert_main_120k.pth`) while training continues 120k→200k in parallel via `--resume_path`. Also: fixed `tools/model_verify/render_report.py` + `checks.py` — 5 of 12 checks were silently missing from the rendered HTML report (SLOW checks with only a summary-text `detail`, no structured `data`); every check now returns structured data and gets a card, generic table for ones without a bespoke chart.
*   [V20_preflopEq Specs — superseded](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V20_preflopEq/specs.md): Range-aware equity front/after fix (opponents already committed this round are guaranteed in, not flat-rolled) + two new features (`equity_edge`, `hand_strength`) + a serious pre-existing bug fix (`vectorize_hand_samples` never got V20's own rescale). `context_dim` 35→37, `contract_version` 4→5. `model_verify --full` @ 75k: 11 PASS/1 WARN/1 FAIL/1 SKIP. SUPERSEDED by V20_preflopEq_AI same day; own checkpoints reused as frozen NN opponents for it.
*   [V20_preflopEq_AI Specs — superseded (was live 2026-07-17)](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V20_preflopEq_AI/specs.md): **WAS LIVE 2026-07-17; superseded** (label corrected 2026-07-21 — V41 is live; this entry's "dropdown default" claim is also obsolete, the dropdown now derives from the registry). (`core/models/v20_preflopEq_AI_engine.py`, `Herocules (v20_preflopEq_AI)`). Same architecture as V20_preflopEq — opponent-pool-only experiment testing whether real NN opponents (lagged self-play + V20_preflopEq's own 25k/50k checkpoints) reduce the shove-preference (see ⭐ Known Shortcomings, `BET-1`). Hypothesis did NOT pan out (sizing diversity faded by 150k) but the model is a clear overall improvement over its parent — `beats_frozen_predecessor` actually ran this time (same architecture, no scale-mismatch skip) and PASSED at +53.5 BB/100, the first validated predecessor win this lineage has managed. `model_verify --full` @ 150k: 12 PASS/1 WARN/1 FAIL/0 SKIP
*   ⭐ [V41 — model_verify report (CURRENTLY LIVE, MILESTONE)](file:///c:/REPO/Antigravity/AIPoker/.agents/skills/OFK/references/V41/model_verify_report.html): **THE LIVE MODEL as of 2026-07-21, and a tagged MILESTONE** — only the second ever after V13 (`milestone=True` in `versions/v41/core/manifest.py` + `versions/v41/MILESTONE.md`; **do NOT delete `versions/v41/weights/expert_main.pth`**). (`core/models/v41_engine.py`, `Herocules (v41)`). Trained 100k hands. `model_verify --full`: **22 PASS / 5 WARN / 0 FAIL / 0 SKIP** — cleanest scorecard of any version (V29 was 21/2/0/1) and the first with zero skips. V40+V41 together **RESOLVED [BET-3]** (3-way aggression at eq 0.65: ~0.01 → 0.81, flat from heads-up); root cause was a simulator bug — a single check ended the betting round, so postflop/BB-option nodes were never in training data at all. `beats_frozen_predecessor` ran as a REAL head-to-head for the first time since the V18 refactor (frozen V40 as an actual NNOpponent): +64.3 BB/100 / 4000 hands (SE ≈ ±13–19, per review #5). 5 WARNs all pre-existing: `nash_pushfold_vs_chart` 78% (a V40 regression V41 did NOT fix — shoves weak suited trash at 5bb where Nash folds), `free_check_low_fold`, `pot_type_sensitivity`, and [OPP-8]'s two flat-response checks. Specs: `versions/v41/SPECS.md`; V40 (the [BET-3] package it builds on): `versions/v40/SPECS.md`..